spring:
  application:
    name: task-manager
  profiles:
    default: dev
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        # reports compare completedAt against UTC bounds; the driver must not shift timestamps
        jdbc:
          time_zone: UTC
        format_sql: true

jwt:
  secret: ${JWT_SECRET}
  # short-lived: a stolen access token is only useful until it expires
  expiration-minutes: ${JWT_EXPIRATION_MINUTES:15}
  refresh-days: ${JWT_REFRESH_DAYS:30}
  # Secure is disabled in dev so the cookie works over plain http on localhost
  refresh-cookie-secure: ${JWT_REFRESH_COOKIE_SECURE:false}

springdoc:
  swagger-ui:
    path: /swagger-ui.html

---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:taskmanager}?serverTimezone=UTC
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD}

---
spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:mysql}:${MYSQL_PORT:3306}/${MYSQL_DATABASE}?serverTimezone=UTC
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}
